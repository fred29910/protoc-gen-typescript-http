// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck
// deno-lint-ignore-file

export type GetShipmentRequest = {
  // Path form 1: /v1/{parent}/shipments/{shipment}
  parent: string | undefined;
  // Path form 2: /v1/shipments/{shipment}
  shipment: string | undefined;
};

export interface ShipmentService {
  GetShipment(request: GetShipmentRequest): Promise<wellKnownEmpty>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (
  request: RequestType,
  meta: { service: string; method: string },
) => Promise<unknown>;

export function createShipmentServiceClient(
  handler: RequestHandler,
): ShipmentService {
  return {
    GetShipment(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (
        request.parent !== undefined && request.parent !== null &&
        request.shipment !== undefined && request.shipment !== null
      ) {
        if (!request.parent) {
          throw new Error("missing required field request.parent");
        }
        if (!request.shipment) {
          throw new Error("missing required field request.shipment");
        }
        const path = `v1/${encodeURIComponent(request.parent)}/shipments/${
          encodeURIComponent(request.shipment)
        }`; // eslint-disable-line quotes
        const body = null;
        const queryParams: string[] = [];
        let uri = path;
        if (queryParams.length > 0) {
          uri += `?${queryParams.join("&")}`;
        }
        return handler({
          path: uri,
          method: "GET",
          body,
        }, {
          service: "ShipmentService",
          method: "GetShipment",
        }) as Promise<wellKnownEmpty>;
      } else if (request.shipment !== undefined && request.shipment !== null) {
        if (!request.shipment) {
          throw new Error("missing required field request.shipment");
        }
        const path = `v1/shipments/${encodeURIComponent(request.shipment)}`; // eslint-disable-line quotes
        const body = null;
        const queryParams: string[] = [];
        if (request.parent !== undefined && request.parent !== null) {
          queryParams.push(
            `parent=${encodeURIComponent(request.parent.toString())}`,
          );
        }
        let uri = path;
        if (queryParams.length > 0) {
          uri += `?${queryParams.join("&")}`;
        }
        return handler({
          path: uri,
          method: "GET",
          body,
        }, {
          service: "ShipmentService",
          method: "GetShipment",
        }) as Promise<wellKnownEmpty>;
      } else {
        throw new Error("no matching binding for GetShipment");
      }
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;

// @@protoc_insertion_point(typescript-http-eof)
